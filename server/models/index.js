const User = require("./User");
const { Journal } = require("./Journal");
const { Entry } = require("./Entry");
const { Legend } = require("./Legend")

module.exports = { User, Journal, Entry, Legend };